package com.soprasteria.movalysmdk.widget.fixedlist;

/**
 * Interface defining a listener to call when an item is added on a MDKFixedList.
 */
public interface FixedListAddListener {

    /**
     * Called when an item is added on the MDKFixedList.
     */
    void onAddClick();
}
